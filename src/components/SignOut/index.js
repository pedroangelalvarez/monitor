import React from 'react';

//import { withFirebase } from '../Firebase';

const SignOutButton = ({ firebase }) => (
  <button style={{color: '#9F2914'}} type="button">
    Sign Out
  </button>
);

export default SignOutButton;
